import React from 'react';

const teamBuilder = () => {
	return <div>team bulder</div>;
};

export default teamBuilder;

// 4 or 8 slots where you can add name and select characters from a list to put them in a team, might do drag and drop, but I dunno about that
